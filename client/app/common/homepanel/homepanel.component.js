import template from './homepanel.html';
import controller from './homepanel.controller';
import './homepanel.scss';

let homepanelComponent = {
  restrict: 'E',
  bindings: {},
  template,
  controller
};

export default homepanelComponent;
